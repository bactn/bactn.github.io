{
  "name": "spa",
  "version": "1.0.0",
  "description": "testing spa",
  "author": "bac.tran",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "http-server": "^0.12.3",
    "nuxt": "^2.0.0",
    "nuxt-buefy": "^0.3.2"
  },
  "devDependencies": {
    "gh-pages": "^3.0.0",
    "push-dir": "^0.4.1"
  }
}
